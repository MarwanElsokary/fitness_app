# .github/workflows/validate-branch-name.yml
name: Validate Branch Name

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  validate-branch:
    name: Validate Branch Name
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v5

      - name: Validate Branch Name
        run: |
          BRANCH_NAME="${{ github.head_ref }}"
          echo "🔍 Checking branch: ${BRANCH_NAME}"

          # Allow main/dev/development/developement branches
          if [[ "$BRANCH_NAME" == "main" || "$BRANCH_NAME" == "master" || "$BRANCH_NAME" == "dev" || "$BRANCH_NAME" == "development" || "$BRANCH_NAME" == "developement" ]]; then
            echo "✅ Branch '${BRANCH_NAME}' is exempt from validation."
            exit 0
          fi

          # Pattern for feature/hotfix/release branches (e.g. feature/FTNS-123-description)
          BRANCH_REGEX='^(feature|hotfix|bugfix|release)\/[A-Z]+-[0-9]+.*$'
          if [[ ! "$BRANCH_NAME" =~ $BRANCH_REGEX ]]; then
            echo "❌ Invalid branch name: '${BRANCH_NAME}'"
            echo "Must match pattern: feature/PROJECT-123-description"
            exit 1
          fi

          echo "✅ Branch name '${BRANCH_NAME}' is valid."
