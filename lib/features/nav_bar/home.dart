import 'package:flutter/material.dart';

class Homeddd extends StatelessWidget {
  const Homeddd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Home dev jh Screen')));
  }
}
